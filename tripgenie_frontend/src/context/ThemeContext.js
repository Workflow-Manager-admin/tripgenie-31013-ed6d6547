/**
 * Kavia AI Documentation:
 * ThemeContext.js - Simple React context for global theme value.
 * Purpose: Used to provide and consume the current theme (dark/light) across the app.
 */

import { createContext } from 'react';

const ThemeContext = createContext();

export default ThemeContext;
